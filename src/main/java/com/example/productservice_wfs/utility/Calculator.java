package com.example.productservice_wfs.utility;

public class Calculator {

    int add(Integer x, Integer y) {
        return x + y;
    }

    double add(Double x, Double y) {
        return x + y;
    }

    int divide(Integer x, Integer y) {
        return x / y;
    }
}
/**
 * In order write test cases -->
 * <p>
 * 1. JUNIT --- this.
 * 2. Mockito --- X
 */
